// app/employer/page.js
import React from "react";

// Tên component là 'page' (chữ thường) là hợp lệ,
// Next.js nhận diện qua tên file 'page.js'.
const page = () => {
  return <div>Employee</div>; // Chỉ trả về đúng nội dung này
};

export default page; // Export default đúng cách
